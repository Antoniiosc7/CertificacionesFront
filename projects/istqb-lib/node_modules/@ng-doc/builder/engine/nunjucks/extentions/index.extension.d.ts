import { Extension } from 'nunjucks';
export declare class NgDocIndexExtension implements Extension {
    tags: string[];
    parse(parser: any, nodes: any): any;
    run(context: any, index: boolean, body: any): any;
}
