"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.TsDocFormatter = void 0;
const tsdoc_1 = require("@microsoft/tsdoc");
const hiddenExcerptKinds = [tsdoc_1.ExcerptKind.BlockTag];
class TsDocFormatter {
    static renderDocNode(docNode) {
        let result = '';
        if (docNode) {
            tsdoc_1.DocBlockTag;
            if (docNode instanceof tsdoc_1.DocExcerpt && !hiddenExcerptKinds.includes(docNode.excerptKind)) {
                result += docNode.content.toString();
            }
            for (const childNode of docNode.getChildNodes()) {
                result += TsDocFormatter.renderDocNode(childNode);
            }
        }
        return result;
    }
    static renderDocNodes(docNodes) {
        let result = '';
        for (const docNode of docNodes) {
            result += TsDocFormatter.renderDocNode(docNode);
        }
        return result;
    }
}
exports.TsDocFormatter = TsDocFormatter;
//# sourceMappingURL=ts-doc-formatter.js.map