import { Observable } from 'rxjs';
import { NgDocBuilderContext, NgDocBuildResult, NgDocPlaygroundMetadata } from '../../interfaces';
import { NgDocEntityStore } from '../entity-store';
import { NgDocEntity } from './abstractions/entity';
import { NgDocCache } from './cache';
import { NgDocPageEntity } from './page.entity';
export declare class NgDocPagePlaygroundEntity extends NgDocEntity {
    readonly store: NgDocEntityStore;
    readonly cache: NgDocCache;
    readonly context: NgDocBuilderContext;
    readonly parent: NgDocPageEntity;
    id: string;
    isRoot: boolean;
    metadata: Record<string, NgDocPlaygroundMetadata>;
    constructor(store: NgDocEntityStore, cache: NgDocCache, context: NgDocBuilderContext, parent: NgDocPageEntity);
    get rootFiles(): string[];
    get buildCandidates(): NgDocEntity[];
    get outputPath(): string;
    build(): Observable<NgDocBuildResult<string, this>>;
    private getMetadata;
    private controlsToProperties;
}
