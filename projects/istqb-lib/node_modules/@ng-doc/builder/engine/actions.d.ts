import { NgDocDemoActionOptions, NgDocDemoPaneActionOptions, NgDocPlaygroundOptions } from '@ng-doc/core';
import { NgDocPageEntity } from './entities/page.entity';
export declare class NgDocActions {
    private readonly page;
    constructor(page: NgDocPageEntity);
    demo(className: string, options?: NgDocDemoActionOptions): string;
    demoPane(className: string, options?: NgDocDemoPaneActionOptions): string;
    playground(playgroundId: string, options?: NgDocPlaygroundOptions): string;
    private performAction;
}
