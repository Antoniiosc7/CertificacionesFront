import { NgDocBuilderOutput } from '../interfaces';
/**
 * Emits the built output to the file system.
 * @param outputs - The outputs to emit.
 */
export declare function emitBuiltOutput(outputs: NgDocBuilderOutput[]): void;
