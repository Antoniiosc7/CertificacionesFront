import { NgDocPageType } from '@ng-doc/core';
import { NgDocPageIndex } from '@ng-doc/core/interfaces';
export interface NgDocIndexBuilderConfig {
    title: string;
    content: string;
    breadcrumbs: string[];
    pageType: NgDocPageType;
    route: string;
}
/**
 *    Builds the indexes for a given content
 * @param config
 */
export declare function buildIndexes(config: NgDocIndexBuilderConfig): Promise<NgDocPageIndex[]>;
