import { NgDocTocItem } from '@ng-doc/app/interfaces';
/**
 * Generate table of contents, only for headings with id
 * @param container
 * @param headings
 */
export declare function generateToc(container: HTMLElement): NgDocTocItem[];
