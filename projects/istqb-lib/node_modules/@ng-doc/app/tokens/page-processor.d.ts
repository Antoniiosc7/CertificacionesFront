import { InjectionToken, Provider } from '@angular/core';
import { NgDocPageProcessor } from '@ng-doc/app/interfaces';
export declare const NG_DOC_PAGE_PROCESSOR: InjectionToken<NgDocPageProcessor<unknown>>;
export declare const NG_DOC_PAGE_CUSTOM_PROCESSOR: InjectionToken<NgDocPageProcessor<unknown>>;
/**
 * Provide a main processor to replace html nodes with an Angular component.
 * Main processors are run before custom processors.
 * @param processors - The processor to provide.
 */
export declare function provideMainPageProcessor<T>(processors: NgDocPageProcessor<T> | Array<NgDocPageProcessor<T>>): Provider[];
/**
 * Provide a processor to replace html nodes with an Angular component.
 * @param processors - The processor to provide.
 */
export declare function providePageProcessor<T>(processors: NgDocPageProcessor<T> | Array<NgDocPageProcessor<T>>): Provider[];
