import { Components, Orama, Schema, SorterConfig, ProvidedTypes } from '../types.js';
interface CreateArguments<A extends ProvidedTypes> {
    schema: Schema;
    sort?: SorterConfig;
    language?: string;
    components?: Components<A>;
    id?: string;
}
export declare function create<A extends ProvidedTypes>({ schema, sort, language, components, id, }: CreateArguments<A>): Promise<Orama<A>>;
export {};
