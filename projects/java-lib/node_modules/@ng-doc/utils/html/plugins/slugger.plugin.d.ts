import { NgDocEntityAnchor, NgDocHeading } from '@ng-doc/core';
import { Root } from 'hast';
/**
 *
 * @param tree
 * @param addAnchor
 * @param headings
 */
export default function sluggerPlugin(addAnchor: (anchor: NgDocEntityAnchor) => void, headings?: NgDocHeading[]): (tree: Root) => void;
