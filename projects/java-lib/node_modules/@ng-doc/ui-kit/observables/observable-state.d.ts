import type { Observable, OperatorFunction } from 'rxjs';
export type StatedObservable<T, E = Error> = Observable<ObservableState<T, E>>;
export interface ObservableState<T, E = Error> {
    result: T | null;
    error: E | null;
    pending: boolean;
}
/**
 *
 * @param retry
 */
export declare function observableState<T, E = Error>(retry?: Observable<unknown>): OperatorFunction<T, ObservableState<T, E>>;
