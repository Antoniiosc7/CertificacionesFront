import { InjectionToken, Provider } from '@angular/core';
import { Constructor } from '@ng-doc/core';
/**
 *
 * @param type
 * @param control
 * @param options
 * @param playgroundId
 * @param selector
 * @param component
 */
export declare function providePlaygroundDemo(playgroundId: string, component: Constructor<unknown>): Provider;
/**
 * Returns the token for the given playground id.
 * @param playgroundId
 */
export declare function getPlaygroundDemoToken<T>(playgroundId: string): InjectionToken<T[]> | undefined;
