"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getPropertyChain = getPropertyChain;
const ts_morph_1 = require("ts-morph");
const class_1 = require("../class");
const interface_1 = require("../interface");
/**
 *
 * @param property
 */
function getPropertyChain(property) {
    const parent = property.getParent();
    const name = property.getName();
    const properties = [];
    if (ts_morph_1.Node.isClassDeclaration(parent)) {
        (0, class_1.forAllClasses)(parent, (cls) => {
            const prop = cls.getProperty(name);
            if (prop) {
                properties.push(prop);
            }
        });
        // TODO: Add interfaces
    }
    if (ts_morph_1.Node.isInterfaceDeclaration(parent)) {
        (0, interface_1.forAllInterfaces)(parent, (int) => {
            const prop = int.getProperty(name);
            if (prop) {
                properties.push(prop);
            }
        });
    }
    return properties;
}
//# sourceMappingURL=get-property-chain.js.map