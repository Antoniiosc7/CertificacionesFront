"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.buildEntityKeyword = buildEntityKeyword;
/**
 *
 * @param key
 * @param title
 * @param path
 * @param anchor
 */
function buildEntityKeyword(key, title, path, anchor) {
    if (anchor.type === 'heading') {
        return {
            key: `${key}#${anchor.anchor}`,
            title: `${title} [${anchor.title}]`,
            path: `${path}#${anchor.anchor}`,
        };
    }
    else {
        return {
            key: `${key}.${anchor.anchor}`,
            title: `${title}.${anchor.title}`,
            path: `${path}#${anchor.anchor}`,
        };
    }
}
//# sourceMappingURL=build-entity-keyword.js.map