import { ClassDeclaration } from 'ts-morph';
/**
 * Returns an array of source files for a component.
 * @param cls - Class declaration.
 */
export declare function getComponentSourceFiles(cls: ClassDeclaration): string[];
