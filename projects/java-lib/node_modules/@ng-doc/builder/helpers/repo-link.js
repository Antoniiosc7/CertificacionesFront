"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.editFileInRepoUrl = editFileInRepoUrl;
exports.viewFileInRepoUrl = viewFileInRepoUrl;
const posix_1 = require("./posix");
/**
 *
 * @param repoConfig
 * @param repoConfig.url
 * @param repoConfig.mainBranch
 * @param filePath
 * @param scope
 * @param lineNumber
 */
function editFileInRepoUrl({ url, mainBranch }, filePath, scope, lineNumber) {
    if (url && mainBranch) {
        const rUrl = url.replace(/\/$/, '');
        const fPath = (0, posix_1.posix)(filePath).replace(/^\//, '');
        return `${rUrl}/edit/${mainBranch}/${fPath}?message=docs(${scope}): describe your changes here...${lineNumber ? `#L${lineNumber}` : ''}`;
    }
    return filePath;
}
/**
 *
 * @param repoConfig
 * @param repoConfig.url
 * @param repoConfig.releaseBranch
 * @param filePath
 * @param lineNumber
 */
function viewFileInRepoUrl({ url, releaseBranch }, filePath, lineNumber) {
    if (url && releaseBranch) {
        const rUrl = url.replace(/\/$/, '');
        const fPath = (0, posix_1.posix)(filePath).replace(/^\//, '');
        return `${rUrl}/blob/${releaseBranch}/${fPath}${lineNumber ? `#L${lineNumber}` : ''}`;
    }
    return filePath;
}
//# sourceMappingURL=repo-link.js.map