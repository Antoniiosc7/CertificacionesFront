import { NgDocConfiguration } from '@ng-doc/builder';
import { NgDocKeyword } from '@ng-doc/core';
import { NgDocEntity } from './entities/abstractions/entity';
export declare class NgDocEntityStore extends Map<string, NgDocEntity> {
    private readonly config;
    private keywordMap;
    private globalKeywords;
    constructor(config: NgDocConfiguration);
    asArray(): NgDocEntity[];
    set(key: string, value: NgDocEntity): this;
    delete(key: string): boolean;
    getByKeyword(keyword: string): NgDocKeyword | undefined;
    getKeywords(): Record<string, NgDocKeyword>;
    loadGlobalKeywords(): Promise<void>;
    updateKeywordMap(): void;
    getAllWithErrorsOrWarnings(): NgDocEntity[];
    getRootEntitiesForBuild(): NgDocEntity[];
    private addKeyword;
}
