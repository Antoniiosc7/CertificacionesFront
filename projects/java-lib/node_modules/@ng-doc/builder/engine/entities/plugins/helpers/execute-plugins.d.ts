import { Observable } from 'rxjs';
import { NgDocEntity } from '../../abstractions/entity';
import { NgDocEntityPlugin } from '../types';
/**
 *
 * @param data
 * @param entity
 * @param plugins
 */
export declare function executePlugins<T, TEntity extends NgDocEntity>(data: T, entity: TEntity, plugins: Array<NgDocEntityPlugin<T>>): Observable<T>;
