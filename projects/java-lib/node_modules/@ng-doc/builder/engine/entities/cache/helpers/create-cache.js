"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createCache = createCache;
const create_cache_for_file_1 = require("./create-cache-for-file");
/**
 * Creates cache for given files, returns object with file path as key and md5 hash as value
 * @param version - version of the current package
 * @param files - list of files, for which cache should be created
 * @param properties - properties, which should be cached
 */
function createCache(version, files, properties) {
    const cache = { version, properties };
    files === null || files === void 0 ? void 0 : files.forEach((filePath) => {
        if (!cache.files) {
            cache.files = {};
        }
        cache.files[filePath] = (0, create_cache_for_file_1.createCacheForFile)(filePath);
    });
    return cache;
}
//# sourceMappingURL=create-cache.js.map